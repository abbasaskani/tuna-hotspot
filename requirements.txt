# Copernicus Marine Toolbox (Python API)
# Pin to a stable release that exists on PyPI (v2.x; v1.x is yanked/ignored by pip)
copernicusmarine==2.2.5

# Core scientific stack
numpy>=1.26,<3.0
scipy>=1.11,<2.0
xarray>=2024.1,<2026.0

# NetCDF I/O (install both engines to reduce "engine not found" issues)
netCDF4>=1.6.5
h5netcdf>=1.3.0
h5py>=3.10

# Plotting (headless OK on GitHub runners)
matplotlib>=3.8,<4.0

# Time handling often required by ocean NetCDF
cftime>=1.6.3

# Small but safe utilities
packaging>=23.2